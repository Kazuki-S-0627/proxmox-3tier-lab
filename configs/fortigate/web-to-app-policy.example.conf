# Public reconstruction of the recorded policy intent.
# The policy ID and object names are deliberately synthetic.

config firewall policy
    edit 0
        set name "web01-to-app01-5000-doc"
        set srcintf "port2"
        set dstintf "port3"
        set srcaddr "web01-doc"
        set dstaddr "app01-doc"
        set action accept
        set schedule "always"
        set service "TCP-5000-doc"
        set nat disable
    next
end
